Real eTools Datamart records, recorded with `manage record_datamart_samples` (contact details
scrubbed). `tests/integrations/test_recorded_samples.py` replays every `*.json` here through the
sync. Re-record after a Datamart change; commit the files.
