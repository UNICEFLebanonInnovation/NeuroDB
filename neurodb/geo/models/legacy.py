"""GENERATED by scripts/gen_legacy_models.py from the v2 schema. Do not edit by hand.

These models map onto the existing NeuroDB tables so that all historical data stays in
place. They are unmanaged in production (LEGACY_MANAGED=False): Django never creates,
alters or drops these tables. Tests set LEGACY_MANAGED=True so the test database can be
built from these definitions.
"""
from django.conf import settings
from django.contrib.postgres.fields import ArrayField
from django.db import models
from django.utils import timezone

LEGACY_MANAGED = getattr(settings, "LEGACY_TABLES_MANAGED", False)

class CadasterLocation(models.Model):
    """Maps to the v2 table ``pivoting_cadasterlocation`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    code = models.CharField(max_length=10, db_index=True)
    gov_code = models.CharField(max_length=10, db_index=True)
    dist_code = models.CharField(max_length=10, db_index=True)
    name = models.CharField(max_length=100, db_index=True)
    polygon_coordinates = ArrayField(models.CharField(max_length=64500, blank=True), blank=True, null=True)
    fill_color = models.CharField(max_length=10, blank=True, null=True, db_index=True)

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_cadasterlocation'
        managed = LEGACY_MANAGED
        verbose_name = 'cadaster location'
        verbose_name_plural = 'cadaster locations'

    def __str__(self):
        return str(self.name) if self.name else f'CadasterLocation {self.pk}'


class DistrictLocation(models.Model):
    """Maps to the v2 table ``pivoting_districtlocation`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    code = models.CharField(max_length=10, db_index=True)
    gov_code = models.CharField(max_length=10, db_index=True)
    name = models.CharField(max_length=100, db_index=True)
    polygon_coordinates = ArrayField(models.CharField(max_length=64500, blank=True), blank=True, null=True)
    fill_color = models.CharField(max_length=10, blank=True, null=True, db_index=True)

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_districtlocation'
        managed = LEGACY_MANAGED
        verbose_name = 'district location'
        verbose_name_plural = 'district locations'

    def __str__(self):
        return str(self.name) if self.name else f'DistrictLocation {self.pk}'


class GovernorateLocation(models.Model):
    """Maps to the v2 table ``pivoting_governoratelocation`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    code = models.CharField(max_length=10, db_index=True)
    name = models.CharField(max_length=100, db_index=True)
    polygon_coordinates = ArrayField(models.CharField(max_length=64500, blank=True), blank=True, null=True)
    fill_color = models.CharField(max_length=10, blank=True, null=True, db_index=True)
    ai_id = models.PositiveSmallIntegerField()

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_governoratelocation'
        managed = LEGACY_MANAGED
        verbose_name = 'governorate location'
        verbose_name_plural = 'governorate locations'

    def __str__(self):
        return str(self.name) if self.name else f'GovernorateLocation {self.pk}'


class SimpleLocation(models.Model):
    """Maps to the v2 table ``pivoting_simplelocation`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    name = models.CharField(verbose_name='Name', max_length=254)
    latitude = models.FloatField(verbose_name='Latitude', blank=True, null=True)
    longitude = models.FloatField(verbose_name='Longitude', blank=True, null=True)
    p_code = models.CharField(verbose_name='P Code', max_length=32, blank=True, default='')
    cas_code = models.CharField(verbose_name='Cas Code', max_length=32, blank=True, default='')

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_simplelocation'
        managed = LEGACY_MANAGED
        verbose_name = 'simple location'
        verbose_name_plural = 'simple locations'

    def __str__(self):
        return str(self.name) if self.name else f'SimpleLocation {self.pk}'


class LocationType(models.Model):
    """Maps to the v2 table ``locations_locationtype`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    created = models.DateTimeField(default=timezone.now, editable=False, verbose_name='created')
    modified = models.DateTimeField(auto_now=True, editable=False, verbose_name='modified')
    name = models.CharField(verbose_name='Name', max_length=254, unique=True)
    admin_level = models.PositiveSmallIntegerField(verbose_name='Admin Level', unique=True, null=True)

    class Meta:
        app_label = 'locations'
        db_table = 'locations_locationtype'
        managed = LEGACY_MANAGED
        ordering = ('name',)
        verbose_name = 'Location Type'
        verbose_name_plural = 'Location Types'

    def __str__(self):
        return str(self.name) if self.name else f'LocationType {self.pk}'


class Location(models.Model):
    """Maps to the v2 table ``locations_location`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    name = models.CharField(verbose_name='Name', max_length=254)
    type = models.ForeignKey('locations.LocationType', verbose_name='Location Type', blank=True, null=True, on_delete=models.CASCADE)
    latitude = models.FloatField(verbose_name='Latitude', blank=True, null=True)
    longitude = models.FloatField(verbose_name='Longitude', blank=True, null=True)
    p_code = models.CharField(verbose_name='P Code', max_length=32, blank=True, default='')
    cas_code = models.CharField(verbose_name='Cas Code', max_length=32, blank=True, default='')
    parent = models.ForeignKey('locations.Location', verbose_name='Parent', blank=True, null=True, related_name='children', on_delete=models.CASCADE)
    is_active = models.BooleanField(verbose_name='Active', blank=True, default=True)
    created = models.DateTimeField(default=timezone.now, editable=False, verbose_name='created')
    modified = models.DateTimeField(auto_now=True, editable=False, verbose_name='modified')
    lft = models.PositiveIntegerField(editable=False)
    rght = models.PositiveIntegerField(editable=False)
    tree_id = models.PositiveIntegerField(db_index=True, editable=False)
    level = models.PositiveIntegerField(editable=False)

    class Meta:
        app_label = 'locations'
        db_table = 'locations_location'
        managed = LEGACY_MANAGED
        unique_together = (('name', 'type', 'p_code'),)
        verbose_name = 'location'
        verbose_name_plural = 'locations'

    def __str__(self):
        return str(self.name) if self.name else f'Location {self.pk}'
