"""GENERATED by scripts/gen_legacy_models.py from the v2 schema. Do not edit by hand.

These models map onto the existing NeuroDB tables so that all historical data stays in
place. Django owns their schema (``managed = True``): the initial migrations describe the tables
as they were, and changes are normal migrations rehearsed on a copy of production first;
``manage ensure_legacy_tables`` creates any table a database never had.
"""
from django.contrib.postgres.fields import ArrayField
from django.db import models
from django.utils import timezone


class ResourceType(models.Model):
    """Maps to the v2 table ``pivoting_resourcetype`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    name = models.CharField(max_length=50)

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_resourcetype'
        managed = True
        ordering = ('name',)
        verbose_name = 'Resource Type'
        verbose_name_plural = 'Resource Types'

    def __str__(self):
        return str(self.name) if self.name else f'ResourceType {self.pk}'


class ResourceTopic(models.Model):
    """Maps to the v2 table ``pivoting_resourcetopic`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    name = models.CharField(max_length=50)

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_resourcetopic'
        managed = True
        ordering = ('name',)
        verbose_name = 'Resource Topic'
        verbose_name_plural = 'Resource Topics'

    def __str__(self):
        return str(self.name) if self.name else f'ResourceTopic {self.pk}'


class ResourceTag(models.Model):
    """Maps to the v2 table ``pivoting_resourcetag`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    name = models.CharField(max_length=50)

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_resourcetag'
        managed = True
        ordering = ('name',)
        verbose_name = 'Resource Tag'
        verbose_name_plural = 'Resource Tags'

    def __str__(self):
        return str(self.name) if self.name else f'ResourceTag {self.pk}'


class Resource(models.Model):
    """Maps to the v2 table ``pivoting_resource`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    title = models.CharField(verbose_name='Title', max_length=500, help_text='The full title of the research')
    description = models.TextField(verbose_name='Description/Abstract', max_length=1500, blank=True, null=True, help_text='A brief narative on the resource objectives.')
    publication_year = models.CharField(verbose_name='Year of publication', max_length=10, help_text='The year the research was published or made available for public use')
    type = models.ForeignKey('pivoting.ResourceType', verbose_name='Resource Type', blank=True, null=True, on_delete=models.SET_NULL)
    topic = models.ForeignKey('pivoting.ResourceTopic', verbose_name='Resource Topic', blank=True, null=True, on_delete=models.SET_NULL)
    section = models.CharField(verbose_name='Program', max_length=100, choices=(('Basic Assistance', 'Basic Assistance'), ('Child Protection', 'Child Protection'), ('Communication', 'Communication'), ('Drivers', 'Drivers'), ('Education', 'Education'), ('Field Operations', 'Field Operations'), ('Field Ops', 'Field Ops'), ('Food Security', 'Food Security'), ('Health and Nutrition', 'Health and Nutrition'), ('Humanitarian Coordination', 'Humanitarian Coordination'), ('Livelihoods', 'Livelihoods'), ('Operations', 'Operations'), ('PRIME', 'PRIME'), ('Palestinian Programme', 'Palestinian Programme'), ('Representative Office', 'Representative Office'), ('SBC', 'SBC'), ('SGBV', 'SGBV'), ('Security', 'Security'), ('Social Policy', 'Social Policy'), ('Social Protection', 'Social Protection'), ('Social Stability', 'Social Stability'), ('WASH', 'WASH'), ('Winter', 'Winter'), ('Youth & Adolescent', 'Youth & Adolescent'), ('Cross-sectoral (Gender, Disability, Youth, etc.)', 'Cross-sectoral (Gender, Disability, Youth, etc.)')))
    resource_link = models.URLField(verbose_name='Resource Link', max_length=2500, blank=True, null=True, help_text='URL link to access the resource')
    resource_file = models.BinaryField(verbose_name='Resource File', blank=True, null=True, editable=True)
    resource_file_name = models.CharField(verbose_name='Resource File', max_length=500, blank=True, null=True)
    resource_image = models.BinaryField(verbose_name='Resource Cover Image', blank=True, null=True, editable=True)
    resource_image_name = models.CharField(verbose_name='Resource Cover Image', max_length=500, blank=True, null=True)
    published = models.BooleanField(default=True)
    tags = models.ManyToManyField('pivoting.ResourceTag', db_table='pivoting_resource_tags', blank=True, verbose_name='tags')

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_resource'
        managed = True
        ordering = ('-id',)
        verbose_name = 'Resource'
        verbose_name_plural = 'Resources'

    def __str__(self):
        return str(self.title) if self.title else f'Resource {self.pk}'


class Map(models.Model):
    """Maps to the v2 table ``pivoting_map`` (read/write, schema owned by the database)."""
    id = models.BigAutoField(verbose_name='ID', primary_key=True)
    created = models.DateTimeField(default=timezone.now, editable=False, verbose_name='created')
    modified = models.DateTimeField(auto_now=True, editable=False, verbose_name='modified')
    name = models.CharField(max_length=500)
    description = models.TextField(blank=True, null=True)
    link = models.URLField(blank=True, null=True)
    status = models.CharField(verbose_name='Status', max_length=1500, blank=True, null=True, default='Draft', choices=(('Draft', 'Draft'), ('In progress', 'In progress'), ('Completed', 'Completed'), ('Archived', 'Archived')))

    class Meta:
        app_label = 'pivoting'
        db_table = 'pivoting_map'
        managed = True
        ordering = ('name',)
        verbose_name = 'map'
        verbose_name_plural = 'maps'

    def __str__(self):
        return str(self.name) if self.name else f'Map {self.pk}'
